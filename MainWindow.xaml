<Window x:Class="FilKollen.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FilKollen.Converters"
        Title="FilKollen - Skydd mot bluff och hackare"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        Height="750"
        Width="1200"
        MinHeight="650"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Style="{DynamicResource FK.Style.MainWindow}"
        Icon="Resources/Icons/filkollen.ico">

    <Window.Resources>
        <converters:WidthToColumnConverter x:Key="WidthToColumnConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Progress bar style för scanning -->
        <Style x:Key="ScanProgressBar"
                TargetType="ProgressBar">
            <Setter Property="Background"
                    Value="{DynamicResource FK.Brush.Surface}"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource FK.Brush.Primary}"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Height"
                    Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="2">
                            <Border x:Name="PART_Track"
                                    Background="Transparent">
                                <Border x:Name="PART_Indicator"
                                        Background="{TemplateBinding Foreground}"
                                        HorizontalAlignment="Left"
                                        CornerRadius="2"/>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Hotrad style -->
        <Style x:Key="ThreatRowStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="{DynamicResource FK.Brush.Surface}"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource FK.Brush.Border}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="CornerRadius"
                    Value="8"/>
            <Setter Property="Margin"
                    Value="0,0,0,8"/>
            <Setter Property="Padding"
                    Value="16,12"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource FK.Brush.Hover}"/>
                    <Setter Property="BorderBrush"
                            Value="{DynamicResource FK.Brush.Primary}"/>
                    <Setter Property="BorderThickness"
                            Value="1.5"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="{DynamicResource FK.Color.Primary}"
                                              Direction="270"
                                              ShadowDepth="2"
                                              BlurRadius="8"
                                              Opacity="0.2"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Sorterbar kolumnheader style -->
        <Style x:Key="SortableHeaderStyle"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="11"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource FK.Brush.Subtext}"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Padding"
                    Value="4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                        Value="True">
                    <Setter Property="Foreground"
                            Value="{DynamicResource FK.Brush.Primary}"/>
                    <Setter Property="TextDecorations"
                            Value="Underline"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Root chrome med FilKollen tema -->
    <Border Background="{DynamicResource FK.Brush.Bg}"
            CornerRadius="16"
            BorderBrush="{DynamicResource FK.Brush.Border}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <!-- Header -->
                <RowDefinition Height="Auto"/>
                <!-- Protection controls -->
                <RowDefinition Height="Auto"/>
                <!-- Status area -->
                <RowDefinition Height="*"/>
                <!-- Content area -->
                <RowDefinition Height="60"/>
                <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- === HEADER === -->
            <Grid Grid.Row="0"
                    Background="Transparent"
                    MouseDown="TopBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo/Brand area - KORRIGERAD hantering enligt krav -->
                <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="24,0,0,0">
                    <!-- Logga (visas ENDAST när den finns, centrerad) -->
                    <Button x:Name="BrandLogo" Background="Transparent" BorderThickness="0" Padding="0" Click="BrandLogo_Click" Visibility="Collapsed"><Image Height="46" SnapsToDevicePixels="True" UseLayoutRounding="True" Stretch="Uniform"/></Button>

                    <!-- Fallback till text (visas ENDAST när INGEN logga finns) -->
                    <StackPanel x:Name="BrandFallback"
                            Orientation="Horizontal"
                            Visibility="Visible">
                        <Border Width="40"
                                Height="40"
                                Margin="0,0,16,0"
                                VerticalAlignment="Center"
                                Background="{DynamicResource FK.Brush.Primary}"
                                CornerRadius="8">
                            <TextBlock Text="FK"
                                    FontWeight="Bold"
                                    FontSize="16"
                                    Foreground="White"
                                       HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="FILKOLLEN"
                                FontWeight="Bold"
                                FontSize="24"
                                   Foreground="{DynamicResource FK.Brush.Primary}"
                                VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <!-- Scanning progress -->
                <StackPanel Grid.Column="1"
                        x:Name="ScanningIndicator"
                        Orientation="Horizontal"
                            VerticalAlignment="Center"
                        Margin="20,0"
                        Visibility="Collapsed">
                    <TextBlock Text="🔍 Skannar..."
                            FontSize="13"
                            Foreground="{DynamicResource FK.Brush.Subtext}"
                               VerticalAlignment="Center"
                            Margin="0,0,12,0"/>
                    <ProgressBar Style="{StaticResource ScanProgressBar}"
                            Width="120"
                            x:Name="ScanProgress"
                            Value="0"/>
                </StackPanel>

                <!-- Window controls -->
                <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="0,0,16,0">
                    <!-- Ljus/Mörk tema toggle -->
                    <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0,0,16,0">
                        <TextBlock Text="☀️"
                                FontSize="14"
                                VerticalAlignment="Center"
                                Margin="0,0,8,0"
                                   Foreground="{DynamicResource FK.Brush.Subtext}"/>
                        <ToggleButton x:Name="ThemeToggle"
                                      Style="{DynamicResource FK.Style.ProtectionToggle}"
                                      Width="50"
                                Height="26"
                                      Checked="ThemeToggle_Checked"
                                      Unchecked="ThemeToggle_Unchecked"/>
                        <TextBlock Text="🌙"
                                FontSize="14"
                                VerticalAlignment="Center"
                                Margin="8,0,0,0"
                                   Foreground="{DynamicResource FK.Brush.Subtext}"/>
                    </StackPanel>

                    <!-- Window controls -->
                    <Button Content="−"
                            Width="36"
                            Height="36"
                            Margin="4,0"
                            Style="{DynamicResource FK.Style.WindowButton}"
                            Click="Minimize_Click"/>
                    <Button Content="□"
                            Width="36"
                            Height="36"
                            Margin="4,0"
                            Style="{DynamicResource FK.Style.WindowButton}"
                            Click="Maximize_Click"/>
                    <Button Content="✕"
                            Width="36"
                            Height="36"
                            Margin="4,0"
                            Style="{DynamicResource FK.Style.WindowButton}"
                            Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <!-- === PROTECTION CONTROLS (Elegant layout från bild 2) === -->
            <Grid Grid.Row="1"
                    Margin="24,0,24,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Auto-skydd panel -->
                <Border Grid.Column="0"
                        Style="{DynamicResource FK.Style.ProtectionCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                Text="AUTO-SKYDD"
                                FontSize="16"
                                FontWeight="SemiBold"
                                   Foreground="{DynamicResource FK.Brush.Text}"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,12"/>

                        <ToggleButton Grid.Row="1"
                                x:Name="ProtectionToggle"
                                      Style="{DynamicResource FK.Style.ProtectionToggle}"
                                      Width="76"
                                Height="38"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,12"
                                      Checked="ProtectionToggle_Checked"
                                Unchecked="ProtectionToggle_Unchecked"/>

                        <TextBlock Grid.Row="2"
                                Text="Söker automatiskt efter hot"
                                   FontSize="12"
                                Opacity="0.7"
                                Foreground="{DynamicResource FK.Brush.Subtext}"
                                   HorizontalAlignment="Center"
                                TextWrapping="Wrap"
                                MaxWidth="160"/>
                    </Grid>
                </Border>

                <!-- IP-Skydd panel -->
                <Border Grid.Column="2"
                        Style="{DynamicResource FK.Style.ProtectionCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                Text="IP-SKYDD"
                                FontSize="16"
                                FontWeight="SemiBold"
                                   Foreground="{DynamicResource FK.Brush.Text}"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,12"/>

                        <ToggleButton Grid.Row="1"
                                x:Name="IpProtectionToggle"
                                      Style="{DynamicResource FK.Style.ProtectionToggle}"
                                      Width="76"
                                Height="38"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,12"
                                      Checked="IpProtectionToggle_Checked"
                                Unchecked="IpProtectionToggle_Unchecked"/>

                        <TextBlock Grid.Row="2"
                                Text="Döljer din IP-adress online"
                                   FontSize="12"
                                Opacity="0.7"
                                Foreground="{DynamicResource FK.Brush.Subtext}"
                                   HorizontalAlignment="Center"
                                TextWrapping="Wrap"
                                MaxWidth="160"/>
                    </Grid>
                </Border>
                <!-- Webbläsarskydd panel -->
                <Border Grid.Column="4"
                        Style="{DynamicResource FK.Style.ProtectionCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                Text="WEBBLÄSARSKYDD"
                                FontSize="16"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource FK.Brush.Text}"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,12"/>

                        <Button Grid.Row="1"
                                x:Name="BrowserCleanTileButton"
                                Content="🌐 Rensa bluffnotiser"
                                Style="{DynamicResource FK.Style.PrimaryButton}"
                                HorizontalAlignment="Center"
                                Padding="16,8"
                                Click="BrowserCleanButton_Click"/>

                        <TextBlock Grid.Row="2"
                                Text="Rensar falska skrämsel-/bluffnotiser i dina webbläsare."
                                FontSize="12"
                                Opacity="0.7"
                                Foreground="{DynamicResource FK.Brush.Subtext}"
                                HorizontalAlignment="Center"
                                TextWrapping="Wrap"
                                MaxWidth="220"/>
                    </Grid>
                </Border>

            </Grid>

                        <!-- === STATUS OCH STATISTIK === -->
                        <Grid Grid.Row="2"
                              Margin="24,0,24,20">
                                <Border Style="{DynamicResource FK.Style.StatusCard}">
                                        <Grid>
                                                <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Status indikator och text -->
                                                <StackPanel Grid.Column="0"
                                                            Orientation="Horizontal"
                                                            VerticalAlignment="Center">
                                                        <Ellipse x:Name="StatusIndicator"
                                                                 Width="20"
                                                                 Height="20"
                                                                 Fill="{DynamicResource FK.Brush.StatusNeutral}"
                                                                 Margin="0,0,16,0"/>

                                                        <StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                        <TextBlock x:Name="StatusMainText"
                                                                                   Text="SYSTEMET ÄR SÄKERT"
                                                                                   FontSize="18"
                                                                                   FontWeight="Bold"
                                                                                   Foreground="{DynamicResource FK.Brush.Text}"/>
                                                                        <!-- Hot-räknare -->
                                                                        <Border x:Name="ThreatCounter"
                                                                                Background="{DynamicResource FK.Brush.Danger}"
                                                                                CornerRadius="16"
                                                                                Padding="12,4"
                                                                                Margin="16,0,0,0"
                                                                                Visibility="Collapsed">
                                                                                <TextBlock x:Name="ThreatCountText"
                                                                                           Text="2 HOT"
                                                                                           FontSize="13"
                                                                                           FontWeight="Bold"
                                                                                           Foreground="White"/>
                                                                        </Border>
                                                                </StackPanel>
                                                                <TextBlock x:Name="StatusSubText"
                                                                           Text="0 hot funna • Auto-skydd inaktivt"
                                                                           FontSize="13"
                                                                           Opacity="0.7"
                                                                           Foreground="{DynamicResource FK.Brush.Subtext}"/>
                                                        </StackPanel>
                                                </StackPanel>

                                                <!-- Statistik -->
                                                <Border Grid.Column="2"
                                                        Background="{DynamicResource FK.Brush.Surface}"
                                                        CornerRadius="8"
                                                        Padding="16,10"
                                                        Margin="16,0">
                                                        <StackPanel>
                                                                <TextBlock Text="SENASTE SKANNING"
                                                                           FontSize="10"
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{DynamicResource FK.Brush.Subtext}"
                                                                           HorizontalAlignment="Center"/>
                                                                <TextBlock x:Name="LastScanText"
                                                                           Text="Aldrig"
                                                                           FontSize="14"
                                                                           FontWeight="Medium"
                                                                           Foreground="{DynamicResource FK.Brush.Text}"
                                                                           HorizontalAlignment="Center"/>
                                                        </StackPanel>
                                                </Border>

                                                <Border Grid.Column="3"
                                                        Background="{DynamicResource FK.Brush.Surface}"
                                                        CornerRadius="8"
                                                        Padding="16,10"
                                                        Margin="8,0">
                                                        <StackPanel>
                                                                <TextBlock Text="HOT HANTERADE"
                                                                           FontSize="10"
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{DynamicResource FK.Brush.Subtext}"
                                                                           HorizontalAlignment="Center"/>
                                                                <TextBlock x:Name="ThreatsHandledText"
                                                                           Text="0"
                                                                           FontSize="14"
                                                                           FontWeight="Medium"
                                                                           Foreground="{DynamicResource FK.Brush.Text}"
                                                                           HorizontalAlignment="Center"/>
                                                        </StackPanel>
                                                </Border>

                                                <!-- Åtgärdsknappar -->
                                                <StackPanel Grid.Column="4"
                                                            Orientation="Horizontal"
                                                            VerticalAlignment="Center"
                                                            Margin="16,0,0,0">
                                                        <Button x:Name="QuickScanButton"
                                                                Content="🔍 Sök efter hot"
                                                                Style="{DynamicResource FK.Style.PrimaryButton}"
                                                                Margin="0,0,12,0"
                                                                Click="QuickScanButton_Click"/>
                                                        <!-- Version info -->
                                                        <TextBlock Text="FilKollen v2.1"
                                                                   FontSize="12"
                                                                   Opacity="0.7"
                                                                   Foreground="{DynamicResource FK.Brush.Subtext}"
                                                                   VerticalAlignment="Center"/>
                                                </StackPanel>
                                        </Grid>
                                </Border>
                        </Grid>
                        <!-- Höger: Version info -->
                    <TextBlock Grid.Column="2"
                            Text="FilKollen v2.1"
                            FontSize="12"
                            Opacity="0.7"
                               Foreground="{DynamicResource FK.Brush.Subtext}"
                            HorizontalAlignment="Right"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>